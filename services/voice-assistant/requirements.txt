fastapi==0.111.0
uvicorn[standard]==0.29.0
# Pinned exactly (not >=): _build_setup_json in live_session.py depends on
# the private google.genai._live_converters module's internal shape, which
# could change without notice on a version bump.
google-genai==2.9.0
firebase-admin>=6.5.0
websockets>=12.0
truststore>=0.9.1
httpx>=0.27.0
