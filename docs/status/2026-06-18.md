# Status — 2026-06-18

## Completed since yesterday

### Security
- Found a real SerpApi key committed in `services/product-matcher/.env.example` (tracked, public-facing file) and replaced it with a placeholder.
- Used `git-filter-repo` to scrub the leaked key from all local git history (every commit, all branches, the stash). Re-added the `origin` remote afterward (filter-repo strips it by default). **Not yet force-pushed** — local history and GitHub's `main` have diverged commit hashes for the same content; still need to force-push `main` and `feature/icon-enhancement` to actually remove the secret from GitHub.
- Generated a new SerpApi key and filled it into `github-secrets-dev`. Still need to confirm the old leaked key is revoked at serpapi.com.
- Added `github-secrets-dev` to `.gitignore`.

### GCP project cleanup
- Discovered the Firebase Console had created a second, unrelated GCP project (`shoplens-dev-prj-ccf98`) instead of attaching Firebase to the already-provisioned `shoplens-dev-prj`.
- Decided to abandon both `shoplens-dev-prj` and `shoplens-dev-prj-ccf98` in favor of a fresh project: **`shoplens-dev-499700`** (project number `935092313069`), confirmed to already have Firebase + billing attached correctly.
- Replaced all `shoplens-dev-prj` references with `shoplens-dev-499700` across 13 files in the repo (docs, READMEs, `.firebaserc`, `.env.example` files, `firebase_options.dart`, etc.).
- Fixed the GitHub org in `docs/shop-lens-cloud-setup.md`'s Workload Identity Federation setup — was `suryaraor`, corrected to the actual org `shoplensai-coder` (would have rejected real GitHub Actions OIDC tokens otherwise).

### GCP provisioning (Part 1 of `docs/shop-lens-cloud-setup.md`) — all done against `shoplens-dev-499700`
- Enabled all required APIs.
- Created `shoplens-runner` service account with all 9 IAM roles + self-impersonation binding.
- Created the `shoplens` Artifact Registry repo (Docker, us-central1).
- Deleted the old `gs://shoplens-dev-hls-segments` and `gs://shoplens-dev-lens-tmp` buckets from the abandoned `shoplens-dev-prj` project (GCS bucket names are globally unique, so this was required to free the names), then recreated both under `shoplens-dev-499700` with public read access, the SA write-access binding, and the 1-day lifecycle rule.
- Created the Firestore database (`FIRESTORE_NATIVE`, us-central1).
- Created the `video-segments-topic` Pub/Sub topic.
- Set up the Workload Identity Federation pool + provider, scoped to `shoplensai-coder/shoplens`.

### Firebase app config
- Retrieved the real Firebase web app config from the console and filled it into `github-secrets-dev` and `frontend/.env.local`.
- Installed `flutterfire_cli` and ran `flutterfire configure` against `shoplens-dev-499700`, which registered new Android and iOS Firebase apps and regenerated `mobile/lib/firebase_options.dart` and `mobile/android/app/google-services.json` with correct keys (the old files had stale keys belonging to the original `cookshop-dev-prj-bd7e2` project from before the CookShop→ShopLens rename).
- `flutterfire configure` registered the iOS app but didn't write `GoogleService-Info.plist` locally (known Windows-side CLI quirk) — pulled the config directly via `firebase apps:sdkconfig ios` instead and saved it to `mobile/ios/Runner/GoogleService-Info.plist`.
- Manually fixed the `web` section of `firebase_options.dart`, which `flutterfire configure` doesn't touch when only targeting `--platforms=android,ios`.

### Verification
- Confirmed `mobile/.env`'s existing Cloud Run URLs are live but belong to an old, unrelated GCP project (`82592393149`) — not a dead link, just the wrong project. Safe to ignore; will be overwritten once the new Cloud Run services are deployed.
- Discovered there's no `.github/workflows/deploy-cloudrun.yml` in the repo at all, despite `docs/shop-lens-cloud-setup.md` referencing it repeatedly. Decided to deploy manually via `gcloud run deploy --source .` for now rather than building the CI/CD workflow today.

### Cloud Run deploy (in progress, not finished)
- Started deploying `ai-analyzer`, `product-matcher`, `state-manager` to `shoplens-dev-499700` from Cloud Shell.
- Hit and resolved: wrong working directory (`cd services/ai-analyzer` failed because Cloud Shell didn't have the repo cloned), `cd ~` landing at filesystem root instead of home, and a GitHub auth failure (password auth no longer supported — resolved via `gh auth login`).
- Repo is now freshly cloned at `~/shoplens` in Cloud Shell with a clean working tree. Deploy commands re-issued; **not yet confirmed complete**.

## TODO for tomorrow

1. **Finish the Cloud Run deploys** of `ai-analyzer`, `product-matcher`, `state-manager` from `~/shoplens` in Cloud Shell; confirm via `gcloud run services list`.
2. Fill in `AI_ANALYZER_URL`, `PRODUCT_MATCHER_URL`, `STATE_MANAGER_URL` in `github-secrets-dev` with the real URLs, and update `mobile/.env` + `frontend/.env.local` to match.
3. Deploy the 4th service, `pubsub-worker` (needs the above URLs as env vars), then create/update the Pub/Sub push subscription pointing at its `/pubsub` endpoint.
4. Confirm the old leaked SerpApi key (`77f052a8...`) has been revoked/deleted at serpapi.com.
5. Decide whether to force-push the rewritten (secret-scrubbed) git history to `origin/main` and `origin/feature/icon-enhancement` — still pending explicit go-ahead since it rewrites shared history.
6. Set up the Cloud Live Stream channel (Part 5 of `docs/shop-lens-cloud-setup.md`) to get `HLS_STREAM_URL`.
7. Paste everything currently staged in `github-secrets-dev` into the actual GitHub repo's **Settings → Environments → dev** (Secrets and Variables).
8. Verify Firebase Authentication (Email/Password) and Hosting are actually enabled for `shoplens-dev-499700` (Part 2, steps 2–3 of the setup doc — not explicitly confirmed yet).
9. (Lower priority) Build the missing `.github/workflows/deploy-cloudrun.yml` CI/CD pipeline so future deploys don't require manual `gcloud` commands.
